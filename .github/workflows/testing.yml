name: CI

on: [push]

env:
  ORG: opendatacube
  IMAGE: geobase
  zzz: "${ORG}/${IMAGE}"
  ghref: ${{ github.ref }}

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - name: Run a one-line script
      run: echo Hello, world!
    - name: Run a multi-line script
      run: |
        echo "ENV TEST: ${ORG}/${IMAGE}:${TAG}"
        echo "ENV TEST2: ${zzz}:${TAG}"
        echo "ghref: ${ghref}"
        echo "ghref: ${{ github.ref }}"
        env | grep "GITHUB_"

      env:
        TAG: 1.3.4-fake
